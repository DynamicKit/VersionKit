
stages:
  - deploy

before_script:
  - gem -v

pages:
  stage: deploy
  script:
  - echo "@Documentation check is there jazzy"
  - cd public && ls && cd ..
  artifacts:
    paths:
    - public
  only:
    - master
