image: ruby:2.7

stages:
  - deploy

before_script:
  - gem install bundler
  - bundle install

pages:
  stage: deploy
  script:
  - echo "Deploy gitlab pages"
  artifacts:
    paths:
    - public
  only:
    - master
